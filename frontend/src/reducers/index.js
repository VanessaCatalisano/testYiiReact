import initialState from "../store/index"
const reducer = (state = initialState, action) => {
    switch(action.type) {

        case "HANDLE_INPUT_CHANGE":
            break;

        case "ADD_NEW_MEMBER":
            break;
    }

    return state;
}